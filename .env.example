# Hosted open-source model (no local model installation required)
LLM_BACKEND=huggingface
LLM_MODEL_NAME=meta-llama/Llama-3.1-8B-Instruct
HF_TOKEN=your_huggingface_token_here

# Keep false to ensure real model calls (no offline stub fallback)
LLM_ALLOW_OFFLINE_FALLBACK=false

# Alternative hosted provider (OpenAI-compatible)
# LLM_BACKEND=openai_compatible
# LLM_API_BASE=https://router.huggingface.co/v1
# LLM_API_KEY=your_huggingface_token_here
# LLM_MODEL_NAME=meta-llama/Llama-3.1-8B-Instruct
